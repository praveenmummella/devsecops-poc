version: 0.2
env:
  parameter-store:
    SONAR_TOKEN: "/devsecops/sonar/token"
  variables:
    APP_PORT: "5000"
    SONAR_PROJECT_KEY: "devsecops-poc"
    ZAP_IMAGE: "ghcr.io/zaproxy/zaproxy:stable"
    ARTIFACT_NAME: "app.jar"
    ZAP_THRESHOLD: "MEDIUM"

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo "Creating ZAP configuration..."
      - mkdir -p zap/reports
      - |
        printf "exclude\t^.*/static/.*\n" > zap/zap.conf
        printf "level\tMEDIUM\n" >> zap/zap.conf
        printf "progress\t100\n" >> zap/zap.conf
        printf "rules\t10011=IGNORE\n" >> zap/zap.conf
        printf "rules\t10015=IGNORE\n" >> zap/zap.conf
        printf "rules\t40012=WARN\n" >> zap/zap.conf
        echo "ZAP config verification:"
        grep -P "exclude\t" zap/zap.conf || echo "ERROR: Tabs missing"
        hexdump -C zap/zap.conf | head

  pre_build:
    commands:
      - export SONAR_TOKEN=$(aws ssm get-parameter --name "/devsecops/sonar/token" --with-decryption --query "Parameter.Value" --output text || echo "")

  build:
    commands:
      - mvn -B clean package

  post_build:
    commands:
      - echo "Starting application..."
      - nohup java -jar target/*.jar --server.port=${APP_PORT} > app.log 2>&1 & echo $! > app.pid
      - sleep 30
      - echo "Running ZAP scan..."
      - >
        docker run --network host --rm
        -v $(pwd)/zap:/zap/wrk:rw
        ${ZAP_IMAGE}
        zap-baseline.py -d
        -t http://localhost:${APP_PORT}
        -c /zap/wrk/zap.conf
        -r /zap/wrk/reports/report.html
        -x /zap/wrk/reports/report.xml
        -j /zap/wrk/reports/report.json
        -l ${ZAP_THRESHOLD} || [ $? -eq 2 ] && echo "ZAP scan completed with warnings"
      - cp target/*.jar ${ARTIFACT_NAME}

artifacts:
  files:
    - "${ARTIFACT_NAME}"
    - "zap/reports/*"
    - "app.log"
  name: "devsecops-artifacts"

cache:
  paths:
    - "/root/.m2/**/*"
